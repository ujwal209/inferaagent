.env
.venv/
__pycache__/
*.pyc
.vscode/
*.sql
test_*.py
# (Keep requirements.txt if needed, but usually it's fine. 
# The user specifically asked for it, so I'll keep it in git.
# But I will definitely ignore .env and the big .sql file)
